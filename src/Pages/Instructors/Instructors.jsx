import { useEffect, useState } from "react";


const Instructors = () => {
  // const [man, setMan] = useState([]);

  // useEffect(() => {
  //   fetch('class.json')
  //   .then(res => res.json())
  //   .then(data => setMan(data))
  // }, [])
  return (
    <div>
      {/* <h2>instructor: {man.length}</h2> */}
    </div>
  );
};

export default Instructors;